function multiply(a: number, b: number): number {
    return a * b;
}
function greet(name: string): string {
    return `Olá ${name}`;
}

const result = multiply(4, 5);
const greeting = greet("João")

console.log(result);
console.log(greeting);
